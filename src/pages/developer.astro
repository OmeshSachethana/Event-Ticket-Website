---
/* No props needed */
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Developer Profile</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --bg-dark: #0b0b1e;
      --bg-card: rgba(255,255,255,0.08);
      --primary: #8b5cf6;
      --secondary: #22d3ee;
      --text-main: #ffffff;
      --text-muted: #b7b7d6;
      --radius: 18px;
    }

    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: radial-gradient(circle at top, #1a1a3a, var(--bg-dark));
      color: var(--text-main);
      line-height: 1.7;
    }

    section {
      padding: 6rem 2rem;
    }

    .container {
      max-width: 1200px;
      margin: auto;
    }

    /* HERO */
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      text-align: center;
    }

    .hero h1 {
      font-size: clamp(3rem, 6vw, 5rem);
      font-weight: 800;
      background: linear-gradient(90deg, var(--primary), var(--secondary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .hero h2 {
      font-size: 1.5rem;
      margin-top: 1rem;
      color: var(--text-muted);
    }

    .hero p {
      max-width: 820px;
      margin: 2rem auto;
      font-size: 1.15rem;
    }

    /* CARD */
    .card {
      background: var(--bg-card);
      backdrop-filter: blur(14px);
      border-radius: var(--radius);
      padding: 2.5rem;
      border: 1px solid rgba(255,255,255,0.15);
      transition: transform 0.3s ease;
    }

    .card:hover {
      transform: translateY(-8px);
    }

    /* GRID */
    .grid {
      display: grid;
      gap: 2.5rem;
    }

    .grid-3 {
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    }

    /* TITLES */
    .section-title {
      font-size: 2.4rem;
      margin-bottom: 2.5rem;
      text-align: center;
    }

    .section-sub {
      text-align: center;
      max-width: 700px;
      margin: 0 auto 4rem;
      color: var(--text-muted);
    }

    /* SKILLS */
    .skill {
      margin-bottom: 1.5rem;
    }

    .skill span {
      display: flex;
      justify-content: space-between;
      font-size: 0.95rem;
    }

    .bar {
      height: 10px;
      border-radius: 20px;
      background: rgba(255,255,255,0.15);
      overflow: hidden;
      margin-top: 0.5rem;
    }

    .bar div {
      height: 100%;
      background: linear-gradient(90deg, var(--primary), var(--secondary));
    }

    /* STATS */
    .stats {
      text-align: center;
    }

    .stats h3 {
      font-size: 3rem;
      color: var(--secondary);
    }

    .stats p {
      color: var(--text-muted);
    }

    /* TIMELINE */
    .timeline {
      position: relative;
      margin-top: 3rem;
    }

    .timeline::before {
      content: "";
      position: absolute;
      left: 50%;
      top: 0;
      bottom: 0;
      width: 2px;
      background: rgba(255,255,255,0.2);
    }

    .timeline-item {
      width: 50%;
      padding: 2rem;
      position: relative;
    }

    .timeline-item:nth-child(odd) {
      left: 0;
      text-align: right;
    }

    .timeline-item:nth-child(even) {
      left: 50%;
    }

    .timeline-item::after {
      content: "";
      position: absolute;
      top: 2.5rem;
      width: 14px;
      height: 14px;
      background: var(--primary);
      border-radius: 50%;
    }

    .timeline-item:nth-child(odd)::after {
      right: -7px;
    }

    .timeline-item:nth-child(even)::after {
      left: -7px;
    }

    footer {
      padding: 3rem;
      text-align: center;
      color: var(--text-muted);
      border-top: 1px solid rgba(255,255,255,0.1);
    }
  </style>
</head>

<body>

  <!-- HERO -->
  <section class="hero">
    <div class="container">
      <h1>Omesh Sachethana</h1>
      <h2>Software Engineer • Full-Stack Developer • Problem Solver</h2>
      <p>
        I build scalable, high-performance applications with clean architecture,
        modern UI, and real-world usability. My focus is engineering solutions
        that are fast, secure, and future-proof.
      </p>
    </div>
  </section>

  <!-- STATS -->
  <section>
    <div class="container grid grid-3">
      <div class="card stats">
        <h3>50+</h3>
        <p>Projects Built</p>
      </div>
      <div class="card stats">
        <h3>3+</h3>
        <p>Years Experience</p>
      </div>
      <div class="card stats">
        <h3>100%</h3>
        <p>Quality Focus</p>
      </div>
    </div>
  </section>

  <!-- SKILLS -->
  <section>
    <div class="container">
      <h2 class="section-title">Technical Expertise</h2>
      <p class="section-sub">
        A balanced blend of frontend precision, backend strength,
        and system-level thinking.
      </p>

      <div class="grid grid-3">
        <div class="card">
          <div class="skill">
            <span><strong>Frontend Engineering</strong><em>90%</em></span>
            <div class="bar"><div style="width:90%"></div></div>
          </div>
          <div class="skill">
            <span><strong>UI / UX Design</strong><em>85%</em></span>
            <div class="bar"><div style="width:85%"></div></div>
          </div>
        </div>

        <div class="card">
          <div class="skill">
            <span><strong>Backend Systems</strong><em>88%</em></span>
            <div class="bar"><div style="width:88%"></div></div>
          </div>
          <div class="skill">
            <span><strong>API Architecture</strong><em>90%</em></span>
            <div class="bar"><div style="width:90%"></div></div>
          </div>
        </div>

        <div class="card">
          <div class="skill">
            <span><strong>Mobile Development</strong><em>80%</em></span>
            <div class="bar"><div style="width:80%"></div></div>
          </div>
          <div class="skill">
            <span><strong>Cloud & Deployment</strong><em>85%</em></span>
            <div class="bar"><div style="width:85%"></div></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- JOURNEY -->
  <section>
    <div class="container">
      <h2 class="section-title">Developer Journey</h2>

      <div class="timeline">
        <div class="timeline-item">
          <div class="card">
            <h3>Strong Foundations</h3>
            <p>Built solid fundamentals in programming, algorithms, and systems.</p>
          </div>
        </div>

        <div class="timeline-item">
          <div class="card">
            <h3>Full-Stack Growth</h3>
            <p>Delivered real-world web and mobile applications.</p>
          </div>
        </div>

        <div class="timeline-item">
          <div class="card">
            <h3>Advanced Engineering</h3>
            <p>Focused on performance, scalability, and maintainability.</p>
          </div>
        </div>

        <div class="timeline-item">
          <div class="card">
            <h3>Professional Impact</h3>
            <p>Building reliable software with measurable business value.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- PHILOSOPHY -->
  <section>
    <div class="container">
      <h2 class="section-title">Engineering Philosophy</h2>
      <div class="card">
        <p>
          Great software is engineered with intention. I value clean code,
          scalable architecture, and thoughtful design. Every product should
          feel simple on the surface while being powerful underneath.
        </p>
      </div>
    </div>
  </section>

  <footer>
    © 2026 • Developer Profile
  </footer>

</body>
</html>
